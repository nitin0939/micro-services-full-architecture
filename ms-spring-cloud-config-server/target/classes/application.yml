spring:
  application:
    name: ms-spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nitin0939/micro-services-full-architecture
          search-paths: environment-variable-repo
          default-label: master



  output:
    ansi:
      enabled: ALWAYS
server:
  port: ${port:8889}



